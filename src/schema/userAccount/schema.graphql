# import Error from "../shared.graphql"

type UserAccount {
  id: ID!
  email: String!
}

type UserAccountOutput {
  userAccount: UserAccount
  errors: [Error!]
}

type Mutation {
  login(email: String!, password: String!): UserAccountOutput
  register(email: String!, password: String!): UserAccountOutput
}
